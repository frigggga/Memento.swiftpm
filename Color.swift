import SwiftUI

extension Color {
    
    static let theme = ColorTheme()
    
}

struct ColorTheme {
    let backgroundColor = Color(.black)
    let themeColor = Color(.purple)
    let brightColor = Color(.white)
    let accentColor = Color(.yellow)

}
